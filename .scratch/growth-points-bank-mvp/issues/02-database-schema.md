# 02 — Database Schema and Migrations

**What to build:** Stand up the PostgreSQL schema exactly as defined in `docs/DETAILED_DESIGN.md §11`, wire up Drizzle ORM with a connection pool, and provide idempotent seed data for dimensions and age-appropriate task templates. After this ticket, `pnpm db:migrate` should bring a fresh Neon database to working state, and `pnpm db:seed` should insert 5 dimensions + ~30 task templates.

**Blocked by:** 01 — Project Scaffolding (needs Drizzle dependency + tsconfig + env).

**Status:** ready-for-agent

- [ ] `drizzle.config.ts` pointing at `DATABASE_URL` env var
- [ ] `src/server/db/schema.ts` with all tables from `DETAILED_DESIGN.md §11` (parents, families, children, dimensions, tasks, checkins, point_transactions, rewards, reward_redemptions, weekly_reviews, growth_diaries, audit_logs)
- [ ] `src/server/db/client.ts` exposing pooled `db` instance
- [ ] Custom `encryptedText` Drizzle type stub (real encryption lands in ticket 10; here it's a no-op pass-through)
- [ ] All CHECK constraints, UNIQUE indexes, and triggers from `DETAILED_DESIGN.md §5`
- [ ] First migration SQL file `0001_init.sql` generated by `drizzle-kit generate`
- [ ] `src/server/db/seed.ts` inserting 5 dimensions (学习力 #2196F3, 运动力 #FF9800, 自控力 #9C27B0, 探索力 #4CAF50, 实践力 #F44336) + 3 task templates per dimension × 3 age groups (6-8, 9-11, 12-14)
- [ ] PGlite in-memory DB config for tests (`tests/setup/db.ts`)
- [ ] All migration tests pass: migrate up → seed → query dimensions returns 5 rows
- [ ] `pnpm db:studio` (Drizzle Studio) opens and shows all tables
